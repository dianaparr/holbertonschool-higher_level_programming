-- Creates a table - On MySQL server
-- Statement for creates a table called force_name in the current database
-- Use of NOT NULL constraint
CREATE TABLE IF NOT EXISTS force_name (id INT,
name VARCHAR(256) NOT NULL);
